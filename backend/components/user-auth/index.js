const authService = require('./services');
const userAuthModel = require('./models');
const authRoutes = require('./routes');

module.exports = {
    authService,
    userAuthModel,
    authRoutes
};
